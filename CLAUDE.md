# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a **GitHub profile README repository** (`x-cristhian-villegas`). It contains a single `README.md` with a personal developer profile page rendered on the GitHub profile. There is no application code, build system, or tests.

## Tech Profile

The developer's stack (as listed in the README): Java, Spring Boot, Angular, TypeScript, Docker, Kubernetes, AWS.

## Notes

- The README uses UTF-16 LE encoding with BOM — preserve this encoding when editing.
- Badge URLs use shields.io; placeholder usernames (`TU_USUARIO`, `tu-perfil`) have not been replaced with actual GitHub/social handles.
